install_data('pishutdown.ui', install_dir: ui_dir)

i18n.merge_file(input: 'pishutdown.desktop.in',
  output: 'pishutdown.desktop',
  type: 'desktop',
  po_dir: '../po',
  install: true,
  install_dir: desktop_dir
)
